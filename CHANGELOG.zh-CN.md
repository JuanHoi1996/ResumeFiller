# 更新日志

本文件用于记录 ResumeFiller 的所有重要变更。

## [2.2.4] - 2026-06-28

### 新增
- **项目经历 · 项目链接**：编辑器新增 `projectLink` 字段；支持识别「项目链接」及飞书招聘等 ATS 的 `data-cy` / `id`（如 `project[0].link`）属性。

## [2.2.3] - 2026-05-30

### 优化
- **扩展图标定稿**：蓝底白字斜体 **R**（以字形中心为轴倾斜、满幅圆角方块），替换旧版小尺寸/多方案草稿资源。

## [2.2.2] - 2026-04-17

### 优化
- **个人信息模块增强**：
  - **基础信息扩充**：增加「紧急联系人」与「紧急联系电话」字段支持。
  - **北森 Beisen 系深度适配**：改进 Scoped Filling 策略，支持跨卡片自动填充身高、体重等字段（点击姓名即可同步填充后续卡片信息）。
- **开放性问答 (openQuestions) 体验优化**：
  - **指向性填充**：重构兜底逻辑，现在会优先填充光标当前所在的输入框，彻底解决在包含多个文本框的页面中总是抢占第一个框的问题。
- **家庭成员模块优化**：支持识别「职业」标签并精准映射至 `familyPosition`，同时优化了单位与职位的识别优先级，防止因标签含糊导致的填错位。

### 文档
- **README**：中英文互链；修正「国聘 / Hotjob 系 / 北森 Beisen」表述（国聘与大易 Hotjob 为不同产品；北森官方英文名一般为 Beisen，页面 DOM 中的 `phoenix` 多为类名）。
- **KNOWN_ISSUES**：移除「中国银行英才极深嵌套表格」条目（待后续版本再验证）。
- **LICENSE**：新增 MIT 许可证。

## [2.2.1] - 2026-04-07

### 优化
- **51job (前程无忧) 系网站深度适配**：针对中粮集团等采用 51job 系统的老旧网申平台进行优化。
  - **标签识别增强**：支持通过 `cname` 和 `ename` 自定义属性识别字段，解决该类系统缺少标准 `<label>` 导致识别失败的问题。
  - **全模块覆盖**：将 `cname/ename` 识别逻辑扩展至实习经历、项目经历、教育经历等所有核心模块。
  - **关键字补全**：在个人信息识别中增加对「身份证」关键字的支持。

## [2.2.0] - 2026-03-31

### 新增
- **新增模块**：「论文发表」与「游戏经历」模块，支持记录论文题目、渠道、作者顺序、游戏列表、成就等专项信息。
- **字段扩充**：教育经历与项目经历增加「起止时间」字段；外语与计算机技能增加「获得日期」字段（如四六级、雅思拿证时间）。
- **个人信息**：增加「身高」、「体重」、「籍贯」、「政治面貌」字段支持。

### 优化
- **产品哲学升级**：秉持“能记就记”原则，即便部分字段（如籍贯、日期等）目前自动填充稳定性存疑，也支持在编辑器中统一管理，方便用户核对辅助。
- **作用域逻辑增强**：论文和游戏经历同样支持「优先匹配 Payload」的 Scoped 识别策略，减少分卡布局下的漏填。
- **开放性问答重构**：该模块改为纯「兜底填充」逻辑，不再依赖关键字，直接填充作用域内第一个可见的长文本框。

## [2.1.6] - 2026-03-30

### 新增
- **基础信息增加身高体重**：在「个人信息」模块中增加了身高（height）和体重（weight）字段，并支持识别对应的网申表单项。

### 修复
- **反馈收集脚本优化**：改进了 `FeedbackCollector.py` 的正则匹配逻辑，采用更稳健的块解析方式，彻底解决多条反馈合并导致 CSV 混乱的问题。

## [2.1.5] - 2026-03-29

### 改进
- **字段识别增强**：在「项目经历」作用域下，支持识别「内容」、「职责」等泛化标签，修复唯品会（Moka系统）等站点的部分项目字段无法填充的问题。
- **UI 交互反馈**：为侧边栏和编辑器中所有按钮增加点击态（`:active`）视觉反馈，提升操作确定感。
- **默认模板更新**：将出厂默认简历数据替换为更具指导意义且幽默风趣的文案。

## [2.1.4] - 2026-03-28

### 修复
- **BOSS 直聘教育经历**：识别「例如：北京大学 / 例如：计算机科学与技术」类占位（学校名以大学/学院等结尾 → `schoolName`，否则 → `major`）；长文本占位含「在校担任/获得荣誉/所学主要课程」→ `educationSummary`。`educations` 作用域与 `projects` 相同优先 `containerHasPayloadField`，并将 `schoolName`/`major` 等纳入教育锚点，避免分卡时 scoped 过早截断。

## [2.1.3] - 2026-03-28

### 修复
- **scoped 项目经历作用域**：`resolveScopedRoot` 对 `projects` 优先采用「当前子树内已能匹配到模板 payload 字段」的容器（`containerHasPayloadField`），再回退到关键词容器（`isLikelyEntryContainer`）。避免仅含项目描述等文案的卡片因关键词先命中而过早截断作用域，导致无法填充同一段经历里另一张卡片上的项目名称/角色（如 BOSS 直聘分卡布局）。

## [2.1.2] - 2026-03-28

### 修复
- **BOSS 直聘（zhipin.com/web/geek/resume）实习/项目经历**：页面仅用「例如: xxx」占位、无真实 label 时识别 `company`/`position` 与 `projectName`/`projectRoleTitle`；实习长文本框按「主要负责 / 序号+负责」等 BOSS 文案映射到 `content`；项目长文避免被「项目经验」误判为 `projectDesc`。作用域检测将 `company`/`position` 计入实习锚点，并补充「实习/工作经历」等关键词以稳定命中卡片容器。

## [2.1.1] - 2026-03-28

### 改进
- **侧栏模板按钮布局**：模板按钮由固定三列 flex 改为 `auto-fill` 网格，允许长标签自动换行，在窄宽度下列数随容器收缩，缓解长文案显得拥挤的问题。

## [2.1.0] - 2026-03-28

### 新增
- **内测反馈**：在可编辑区域右键菜单增加「ResumeFiller · 上报此输入框」，一键将当前输入框的 URL、标签/占位/上下文提示与 `outerHTML`（过长会截断）复制到剪贴板，便于反馈识别失败问题。

## [2.0.2] - 2026-03-23

### 修复
- **国聘 / WeRecruit 自我评价 placeholder 抢占问题**：修复隐私提示占位文案（如含 `手机号/邮箱/联系方式`）在非 `personalInfos` 模块中误触发个人信息字段映射（`phone/email`），导致 `selfEvaluation` 无法填充的问题。

## [2.0.1] - 2026-03-23

### 修复
- **WeRecruit / 招商证券**：修复 `familyMembers` 在“家庭关系”卡片省略上下文定语时的识别（如仅显示 `姓名/工作单位/职位`）。禁用跨 section 的直接映射，确保能正确映射到 `familyName/familyCompany/familyPosition`。
- **WeRecruit / 招商证券**：支持自我评价 label `评价内容`（映射到 `selfEvaluation`）。
- **WeRecruit / 招商证券**：支持实习经历 label `企业名称`（映射到 `company`）。
- **WeRecruit / 招商证券**：修复实习经历 `content` 误填到教育“专业描述”类字段（label like `专业描述`）。

## [2.0.0] - 2026-03-23

### 变更
- **基于意图的作用域填充（v2 重构）**：
  - `detectField` 新增显式 `section`（意图）参数，并将字段映射锁定在当前选中的模块。
  - `autoFill` 通过模板 payload keys 过滤填充目标，降低跨模块误填概率。
  - `scoped` 模式已覆盖 `personalInfos`。
- **BOSS（zhipin.com）安全性增强**：收紧长文本兜底策略，避免覆盖其它容器。

### 修复

## [1.6.10] - 2026-03-20

### 修复
- **星星充电（北森 Phoenix）项目名称识别**：加强 `getLabelText/getContextHintText` 的 Phoenix label 获取兜底，确保 `项目名称` 等字段稳定命中。

## [1.6.9] - 2026-03-20

### 修复
- **星星充电（北森 Phoenix）label 提取回归稳健**：收紧 Phoenix label 查询范围到 `.form-item__title label.form-item__text`，避免同组 label 抓取导致项目名称/项目角色等字段回归失配。

## [1.6.8] - 2026-03-20

### 修复
- **星星充电（北森 Phoenix）项目角色（label=职务）识别**：优先从 Phoenix 同组 `.fields-row` 采集 label 标题，增强 `extendedText` 的项目上下文；并基于 `项目名称/项目描述/...` 锚点识别 `projectRoleTitle`。

## [1.6.7] - 2026-03-20

### 修复
- **星星充电（北森 Phoenix）项目角色/项目描述**：
  - 增强 Phoenix 同组 label 标题采集，使 “职务”(项目角色) 在项目语境下可正确识别为 `projectRoleTitle`。
  - 修正 “项目描述” 映射到通用 `content`，避免误填短的 “项目介绍”。

## [1.6.6] - 2026-03-20

### 修复
- **星星充电（北森系）项目经历“项目角色”识别**：当项目角色 label 使用“职务/职位/岗位”且上下文属于“项目”时，优先识别为 `projectRoleTitle`，避免被误识别为实习经历 `position`。

## [1.6.5] - 2026-03-20

### 修复
- **星星充电（北森 Phoenix）字段识别增强**：兼容 `form-item--phoenix` 下 `form-item__title > label.form-item__text` 的 label 结构，解决实习/项目/证件号等字段大量无法识别与填充问题。
- **补充开始/结束时间识别**：优先根据 label（如“开始时间/结束时间”）识别 `start/end`，避免依赖 placeholder（常为“请选择”）。

## [1.6.4] - 2026-03-20

### 修复
- **星星充电（北森系）表单识别增强**：补充 Phoenix/Beisen 组件的标签提取逻辑（`form-item__label`、`aria-labelledby`），修复“证件号、教育经历、实习经历、项目经历大量字段无法识别填充”的问题。

## [1.6.3] - 2026-03-17

### 修复
- **特来电系统“部门”字段可识别**：补充 `部门/请输入部门` 的识别映射，确保可正常自动填充。

## [1.6.2] - 2026-03-17

### 新增
- **教育经历新增“专业”字段**：支持在编辑器维护并在网申表单中自动识别填充。

## [1.6.1] - 2026-03-17

### 新增
- **基础信息新增“户籍所在地”字段**：支持在编辑器维护并在网申表单中自动识别填充。

## [1.6.0] - 2026-03-17

### 新特性
- **新增“开放性问答 (AI Writing)”模块**：
  - 专门用于处理“职业生涯规划”、“应聘理由”、“性格优缺点”等网申小作文。
  - 支持在编辑器中维护多个版本的回答模板。
  - 智能识别长文本框（Textarea）及包含“规划/理由/陈述”等关键词的字段。
  - 默认内置了针对用户背景量身定制的“量化版”与“投行版”职业规划模板。

## [1.5.2] - 2026-03-17

### 修复
- **家庭成员逻辑大修**：
  - **优先级纠偏**：将家庭成员识别逻辑置于实习经历之前，解决“单位/职务”被误识别为实习字段的问题。
  - **关系互斥校验**：增加“关系守卫”逻辑。当填充“父亲”信息时，插件会自动检查标签，若标签明确包含“母亲/配偶”等其他关系词，则会自动跳过，防止全家信息被同一个人覆盖。

## [1.5.1] - 2026-03-17

### 修复
- **增强家庭成员识别**：新增“父亲/母亲/配偶/子女”等直系亲属关键词作为识别触发词，适配不带“家庭成员”标题的扁平化表单结构。

## [1.5.0] - 2026-03-17

### 新特性
- **新增“家庭成员”模块**：
  - 支持在编辑器中维护多条家庭成员信息（关系、姓名、单位、职务、联系电话、政治面貌）。
  - 在弹出菜单中增加家庭成员快捷填充入口，支持 Scoped 模式（点哪填哪）。
  - 增加对常见家庭成员表单字段的模糊匹配识别逻辑。

## [1.4.0] - 2026-03-17

### 变更 (里程碑版本：稳健性重构)
- **识别引擎大幅稳健化**：
  - 恢复了保守的上下文清理名单和扫描深度（回归至 v1.2.7 的稳健水平），彻底根除“全页误填”问题。
  - 放弃对中国银行（英才）等极端嵌套表格系统的 100% 自动适配，改为优先保证 95% 主流招聘系统的填充精度。
- **保留所有核心增强字段**：虽然引擎回归稳健，但依然保留了“离职原因”、“学院/院系”、“外语能力”、“计算机技能”及“证明人完整信息”的所有字段识别。
- **Scoped 模式优化**：针对识别失败的顽固字段，建议结合插件的“作用域填充”模式（点哪里填哪里）使用。

### 修复
- 优化了证明人姓名与职务的互斥逻辑，减少在散装表格中的错位几率。

## [1.3.5] - 2026-03-17

### 修复
- **精准识别证明人信息**：修正了在复杂表格布局中，证明人姓名被误填为职务的问题。
  - 优化判定逻辑：现在只有在直接标签包含“单位/职务”时才填充职务，防止跨行的“担任职务”标签干扰下方证明人姓名的识别。
- 提高了在中国银行（英才）系统这种高度嵌套表格环境下的字段稳定性。

## [1.3.4] - 2026-03-17

### 修复
- **解决上下文劫持问题**：重构识别逻辑顺序，确保“直接标签（Label）”拥有最高优先级。
  - 彻底解决了中行系统中“离职原因”关键词劫持下方证明人姓名、单位字段的问题。
- **强化证明人识别**：将证明人相关信息的 Fallback 匹配提到最高优先级，确保在复杂表格布局中即便存在多个干扰关键词也能准确识别。
- **优化离职原因识别**：增加了对证明人上下文的显式排除。

## [1.3.3] - 2026-03-17

### 新增
- 实习经历模块新增“离职原因（`leaveReason`）”字段支持。
- Editor 编辑器同步支持离职原因字段的 CRUD 操作。

### 修复
- **优化 `position` 识别逻辑**：显式排除包含“离职”关键词的字段，彻底解决中行系统中“离职原因”被误填为职位名称的问题。
- 进一步微调识别优先级，确保“证明人姓名”与“证明人单位/职务”在复杂布局下更精准地命中。

## [1.3.2] - 2026-03-17

### 修复
- **重构识别优先级（重要）**：引入“直接标签优先”原则，彻底解决中行系统出现的“全页误填公司名”问题。
  - 插件现在会优先根据输入框旁边的直接标签进行识别。
  - 只有在直接标签不确定时，才会参考上下文信息。
- **精细化 `company` 匹配**：增加了多个排除词，防止“单位”上下文干扰到职位、证明人、电话等字段。
- 修复了“证明人”姓名在复杂表格布局中被误匹配导致无法填充的问题。

## [1.3.1] - 2026-03-17

### 修复
- 彻底解决中国银行（英才系统）实习经历中“联系电话”因表格层级太深导致的识别失效问题。
- **深度加固上下文扫描**：将 `getContextHintText` 上溯深度由 2 层增加至 **3 层**，确保能扫描到表格布局中的 `<tr>` 兄弟节点。
- **优化字段冲突**：在 `content`（工作内容）识别中增加了对证明人上下文的显式排除，防止证明人信息误填入工作描述框。

## [1.3.0] - 2026-03-17

### 新增
- 实习经历模块新增“证明人单位/职务（`refereeCompanyTitle`）”独立字段支持。
- Editor 编辑器同步支持证明人单位/职务字段的 CRUD 操作。

### 修复
- **重构字段识别逻辑**：大幅精简了 `detectField` 的上下文自动清理名单。
  - 核心工作/项目字段（公司、职位、描述、电话、地点等）现在会永久保留上下文。
  - 解决了在中国银行等布局复杂的系统中，“联系电话”因触发其他描述类标签而丢失“证明人”上下文的问题。
- 优化了证明人信息的映射准确度，防止信息错位填充。

## [1.2.9] - 2026-03-17

### 修复
- 修正 `detectField` 的上下文清理逻辑：姓名、手机、邮箱等字段不再强制清空上下文。
  - 解决了中国银行（英才）系统中“证明人联系电话”无法识别的问题。
- 调整识别优先级：确保 `refereePhone`（证明人电话）的识别优先级高于个人 `phone`。
- 优化字段匹配逻辑，提高在散装/表格布局表单中的稳健性。

## [1.2.8] - 2026-03-17

### 修复
- 进一步增强中国银行（英才）系统实习经历填充兼容性。
- **优化字段识别**：新增 `职务` 识别为职位名称（`position`），并排除证明人相关的干扰项。
- **优化上下文扫描**：`getContextHintText` 现支持扫描前置 3 个兄弟节点。
  - 解决了“证明人”标签在散装表单（如中行表格）中因层级太深导致其下方“联系电话”无法正确识别的问题。
- **支持证明人单位/职务**：由于中行没有专门的证明人单位字段，`refereePhone`（联系方式）现已支持这类复合型文本框的填充。

## [1.2.7] - 2026-03-17

### 新增
- 教育经历模板新增“学院（College）”字段支持。
- 优化字段识别：新增对 `院系`、`学院`、`学部` 标签的自动识别。
- Editor 编辑器同步支持学院字段的 CRUD 操作。

## [1.2.6] - 2026-03-17

### 修复
- 显著提升中国银行（BOC/英才）及其他传统表单系统的字段识别精度。
- **解决上下文污染（Context Bleed）**：收缩 `getContextHintText` 追溯范围，并对跨字段文本长度增加约束。
- **优化标签识别**：`getLabelText` 支持识别以冒号结尾的 `SPAN/DIV/TD` 文本标签，减少对上下文的依赖。
- **强化冲突排除**：在姓名、邮箱、手机号识别中增加互斥关键词（例如手机识别中排除“邮箱”干扰，姓名识别中排除“证件”干扰）。

## [1.2.5] - 2026-03-17

### 修复
- 增强工商银行（ICBC）官网实习经历表单兼容性。
- 优化 `position`（职位）识别逻辑：排除“级别”关键词（修复“岗位级别”误填）。
- 扩展 `content`（工作内容）识别范围：新增“主要业绩”作为内容填充的有效标签。
- 更新 `fillGeneralWorkContentFallback` 通用兜底逻辑，加入“主要业绩”识别。

## [1.2.4] - 2026-03-17

### 新增
- 新增 `languages`（外语能力）和 `computerSkills`（计算机技能）识别与自动填充。
- 在 `data.js` 中增加了外语与计算机技能的默认示例模板。
- 为“自我评价”、“外语能力”、“计算机技能”模块新增“作用域（Scoped）”填充支持。
  - 现在点击这些区域的输入框再点击填充，将仅填充当前关联的输入框，防止覆盖页面其他字段。

### 修复
- 优化 Feishu 表单的“描述”标签兼容性逻辑。

## [1.2.3] - 2026-03-17

### 修复
- 增强飞书（Feishu/Lark）招聘平台兼容性（如广发基金等）。
- 新增对 `描述` 标签的通用识别，映射到实习/项目经历 `content` 字段。
- 优化实习经历区块识别（`描述` 现已加入容器定位关键字）。

## [1.2.2] - 2026-02-25

### 新增
- 个人信息模板与 Editor 表单新增 `家庭住址` 字段。

### 修复
- 新增对 `项目内容` 标签的兼容识别（映射到项目通用 `content` 字段）。

## [1.2.1] - 2026-02-25

### 修复
- 新增“作用域填充”模式，针对重复表单页面仅填充当前编辑块（实习/项目/教育）。
- 修复 Moka 类页面项目经历标签使用 `项目经验` 时的自动填充识别问题。
- 修复 Moka 类页面项目区块字段定位（`项目名称` 与通用项目描述映射）。
- 已验证远景智能招聘官网（Moka）全流程自动填充可用。

## [1.2.0] - 2026-02-25

### 新增
- 新增 `personalInfos` 模板分组，支持自动填充：
  - 姓名
  - 手机号
  - 邮箱
  - 证件号码
- 新增 `educations` 模板分组，支持自动填充：
  - 学校名称
  - 在校经历
  - 核心课程
- Editor 扩展为可对以下类型执行 CRUD：
  - 个人信息
  - 教育经历
- Popup 增加“个人信息/教育经历”模板快捷填充区块。

### 变更
- 优化字段识别，区分“个人联系方式”与“证明人联系方式”。
- 扩展文本输入事件触发范围，覆盖教育经历相关文本字段。

## [1.1.0-beta.1] - 2026-02-25

### 新增
- 基于 EasyResume 创建通用版插件 `ResumeFiller`。
- 新增 `storage.js`，统一运行时数据源到 `chrome.storage.local`。
- 新增 `editor.html` + `editor.js`，支持在插件内编辑简历数据。
- 新增以下数据类型的 CRUD：
  - 实习经历
  - 项目经历
  - 自我评价
- 新增 Editor 页 JSON 导入/导出功能。
- 新增 EasyResume 转 ResumeFiller 的导入模板：`easyresume-import.json`。

### 变更
- 在 `data.js` 中用通用默认数据替换个人数据。
- 在 manifest、popup 与日志中统一产品名为 `ResumeFiller`。
- popup 模板渲染改为 storage 优先读取，不再仅依赖静态 `data.js`。
- content 自动填充流程改为 storage 优先读取简历数据。

### 修复
- 修复 `职责业绩` 等标签无法命中自动填充的问题。
- 新增对自我评价标签 `优势亮点` 的识别。
- 修复 BOSS `工作内容` 误填到 `工作业绩` 的问题。
- 限制 `工作业绩/业绩成果` 不再作为 `content` 自动填充目标。

---

## 版本策略
- Patch（`x.y.Z`）：兼容性修复与缺陷修复。
- Minor（`x.Y.0`）：向后兼容的功能新增。
- 说明：Chrome 扩展 `manifest.version` 必须是纯数字点分格式。
