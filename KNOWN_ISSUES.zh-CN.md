# 已知问题

本文件用于记录当前已知限制及临时规避方案。

## 1) 站点标签命名漂移
- **现象**：同一语义字段在不同站点标签名称差异较大。
- **影响**：部分文本框/下拉框首次可能命中失败。
- **临时方案**：先滚动到目标区块再触发填充；若仍失败，手填一次并反馈字段标签+截图。

## 2) 复杂下拉组件兼容性
- **现象**：行业/单位性质/工作性质等在部分自定义下拉中无法稳定选中。
- **影响**：字段会高亮但未真正完成选择。
- **临时方案**：该字段手动选择，其余字段继续自动填充。

## 3) 日期自动填充当前默认静音
- **现象**：开始/结束日期可能保持空白。
- **原因**：为避免不同站点日期控件格式不一致导致误填。
- **临时方案**：自动填充后手动补日期。

## 4) 当前包不支持 Safari 直接安装
- **现象**：Chrome 扩展包不能直接在 Safari 加载。
- **临时方案**：优先在 Chrome/Edge 使用；Safari 适配建议单独立项。

## 5) 工作内容与工作业绩分离策略
- **现象**：部分站点同时存在 `工作内容` 与 `工作业绩`。
- **当前策略**：为避免误填，插件默认不自动填 `工作业绩` 字段。

## 6) BOSS（zhipin.com）内容与 Scoped 填充限制
- **现象**：
  - 实习经历：通常只能稳定填充基础字段（公司/部门/职位）；部分实习长文本字段可能无法成功写入。
  - 项目经历：长文本会在“项目描述（长内容）”与“项目介绍/背景（短内容）”之间出现分配不正确，可能导致其中一个容器保持空白。
  - 自我评价与教育经历：在部分页面/组件上可能无法填充（或仅部分填充）。
- **临时方案**：手动输入，或先点进具体目标输入框再使用插件 Scoped Fill。如果仍然失败，请记录该字段标签 + 截图并反馈，以便后续针对性修复。

## 7) 51job (前程无忧/xyz.51job.com) 自定义下拉框与“其他”字段误填
- **现象**：
  - **下拉框兼容性**：学校、专业等字段在 51job 系网站中常采用自定义下拉组件，插件目前无法直接模拟点击选择。
  - **“其他”字段误填**：当主要字段为下拉框无法填充时，插件可能会识别到下方的“其他学校”、“其他专业”等文本输入框并填入内容，导致数据错位。
- **原因**：51job 系网站的 DOM 结构较为陈旧且使用了大量自定义脚本控制下拉菜单，目前采用保守策略，不强制干扰其内部下拉逻辑。
- **临时方案**：手动从下拉框中选择正确项，并清空“其他”文本框中误填的内容。

## 8) 同花顺校招（campus.10jqka.com.cn）GitHub / Scholar 主页
- **现象**：
  - 「Github主页地址」「Scholar主页」等字段曾被「地址」类兜底误识别为家庭住址（已做排除，不再误填住址）。
  - 正确写入 GitHub / Scholar URL 在部分页面仍不稳定（识别到字段但未成功写入，或依赖编辑器新字段 `githubUrl` / `scholarUrl` 是否已保存）。
- **影响**：网络主页类 URL 需手填；基础信息其它字段仍可自动填充。
- **临时方案**：在目标输入框中手动粘贴 GitHub / Scholar 链接。编辑器「基础信息」中可预先记录这两个字段，便于核对。
- **反馈样本**：`campus.10jqka.com.cn` 简历页，标签「Github主页」「Scholar主页」（Element UI `el-input`）。

## 9) 国金证券 / 北森（gjzq.zhiye.com）实习经历 Scoped 按「行」截断
- **现象**：实习经历常见 **3 排 × 约 2 列** 布局（如「单位名称 / 职位名称」「实习内容 / 证明人」「证明人职务 / 证明人联系方式」）。focus 某一输入框后再点实习模板，Scoped Fill 往往只填**当前这一排**的字段，需换行再点共约 3 次才能填完一块实习经历。
- **原因**：北森 Phoenix 表单常把同一排字段包在同一行级容器里；Scoped 自焦点向上找「条目容器」时，容易在**行容器**就停住，而不是整段实习经历的共同祖先。字段本身可识别（单位名称、职位名称、实习内容、证明人等），并非完全填不了。
- **影响**：需多次点击；不阻塞手填或分行 Scoped。
- **临时方案**：每一排先点进任意输入框，再点一次同一实习模板；或不用 Scoped、在页面空白处失焦后视站点表现再试整页填充。
- **反馈样本**：`gjzq.zhiye.com` 表单页（Beisen / `phoenix-input`），实习相关标签见上。
