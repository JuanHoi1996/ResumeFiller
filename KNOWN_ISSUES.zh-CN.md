# 已知问题

当前仍存在的限制与可用规避方式。已修复的行为记在 CHANGELOG，不在本文件重复。

## 1) 站点标签命名差异
- **现象**：同一语义字段在不同站点的标签文案差异较大。
- **影响**：部分文本框或下拉框可能一次填不上。
- **规避**：先滚动到目标区块再填充；仍失败时，点进该输入框后再次点击同一模板（Scoped Fill），或手填后反馈字段标签与截图。

## 2) 复杂自定义下拉
- **现象**：行业、单位性质、工作性质等字段在部分自定义下拉中无法稳定选中。
- **影响**：字段可能被高亮，但选项未真正选上。
- **规避**：该字段手动选择；其余已填字段可保留。

## 3) 日期默认不自动填写
- **现象**：开始/结束日期可能保持空白。
- **原因**：各站点日期控件格式不一致，自动写入容易写错。
- **规避**：自动填充后手动补日期。

## 4) 不支持在 Safari 中直接安装
- **现象**：当前包是 Chrome Manifest V3 扩展，不能直接在 Safari 加载。
- **规避**：使用 Chrome 或 Edge（及其它可加载未打包 Chromium 扩展的浏览器）。

## 5) 「工作业绩」故意不填
- **现象**：部分站点同时有「工作内容」和「工作业绩」。
- **当前行为**：默认不自动填写「工作业绩」，以免把职责描述写入业绩栏。

## 6) BOSS 直聘（zhipin.com）长文本与 Scoped Fill
- **现象**：
  - 实习经历：公司、部门、职位等基础字段较稳；部分实习长文本可能写不进。
  - 项目经历：长文本在「项目描述」与「项目介绍/背景」之间可能分错，其中一个会空着。
  - 自我评价与教育经历：部分页面只能部分填充或完全填不上。
- **规避**：手填，或先点进目标输入框再使用对应模块的 Scoped Fill。仍失败时可反馈字段标签与截图。

## 7) 前程无忧（xyz.51job.com）自定义下拉与「其他」框
- **现象**：
  - 学校、专业等常为自定义下拉，插件无法模拟点选。
  - 主下拉填不上时，可能把内容写进下方的「其他学校」「其他专业」文本框。
- **原因**：页面用较老的 DOM 和自定义脚本控制下拉，插件不强制改其内部逻辑。
- **规避**：从下拉中手选正确项，并清空「其他」框里误填的内容。

## 8) 同花顺校招（campus.10jqka.com.cn）GitHub / Scholar 主页写入不稳定
- **现象**：标签如「Github主页」「Scholar主页」可以识别，但 URL 有时写不进输入框（需编辑器「基础信息」里已保存 `githubUrl` / `scholarUrl`）。
- **影响**：这两个主页字段可能仍需手贴；其它基础信息仍可自动填。
- **规避**：在目标框中手动粘贴链接。
- **样本**：`campus.10jqka.com.cn` 简历页，Element UI `el-input`。

## 9) 农业银行招聘站：桌面页按移动端交互，滚轮与右键不可用
- **现象**：页面把桌面端做成触控逻辑——鼠标滚轮几乎无效，右键菜单被拦截。侧栏填充难以对准字段；右键「复制字段报告」也无法弹出。
- **可能原因**：全屏手势层、自定义滚动容器，或页面阻止了 `contextmenu`。
- **影响**：该站目前无法稳定自动填充，也无法用字段报告采集页面结构。
- **规避**：若 Tab 或单击仍能聚焦输入框，可尝试 Scoped Fill；否则手填。反馈时请附招聘页网址；浏览器「另存为网页」通常复现不了滚轮/右键被拦截的行为。
